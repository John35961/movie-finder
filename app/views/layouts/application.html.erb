<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <nav class="position-sticky top-0 navbar text-light bg-dark p-3">
      <p class="mb-1 fs-3"><%= link_to 'Movie finder', lists_path, class: 'text-light text-decoration-none'  %></p>
      <% unless current_page?(controller: 'lists', action: 'new') %>
          <%= link_to 'Add list', new_list_path, class: 'btn btn-primary' %>
      <% end %>
    </nav>
    <div class="container my-4 p-3">
      <div class="row d-flex justify-content-center">
        <div class="col-12 col-lg-8">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
